var x = 10;
var y = 1 + 25 * 3 / 2;
var z int = 33 / 29 - 75 + 89 - 23 / 78 / 89;

func add(x int, y int) int {
	return x + y
}

func split(sum int) (x, y int) {
	x = sum * 4 / 9
	y = sum - x
	return
}


package main

import (
	"fmt"
	"math/cmplx"
)

var (
	ToBe   bool       = false
	MaxInt uint64     = 1<<64 - 1
	z      complex128 = cmplx.Sqrt(-5 + 12i)
)

func main() {
	const f = "%T(%v)\n" // test
	fmt.Printf(f, ToBe, ToBe)
	for() {
		fmt.println	("hello");
	}
	fmt.Printf(f, MaxInt, MaxInt)
	fmt.Printf(f, z, z)
}