package main;

const a, b = 1, 1.0;
const c string = "one-point-zero";

var i int = 0;
var j float = b - 2.0;

func main() {
    var i, j, k, n int;
    var c, d = 2, 3;

    for y := 0; y < n; y++ { ### TODO ###
        j := (2.0 * 3.45 - 1.57);
        b = b * j;
    };

    if a < i || c < j && d < k {
        x := a + 2;
    };

    var x = a < c;
};