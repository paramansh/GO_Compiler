REQUIREMENTS:
1. sudo apt install gcc-multilib

RUN:
$ ./exec <testcase>

INSTRUCTIONS:
1. In function main, don't write return statement.
2. In all other functions - void or otherwise - return statement is necessary.
3. Print of immediate operands is not supported.
4. Do not scan integers into non-identifiers.
